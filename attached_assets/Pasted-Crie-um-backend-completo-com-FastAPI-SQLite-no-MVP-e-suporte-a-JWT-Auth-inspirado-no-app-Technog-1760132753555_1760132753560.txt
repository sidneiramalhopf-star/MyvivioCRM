Crie um backend completo com FastAPI, SQLite (no MVP) e suporte a JWT Auth, inspirado no app Technogym, adaptado ao conceito Metavida (saúde integral: corpo, mente e energia).

⚙️ Estrutura principal do projeto:
- app.py (FastAPI principal com CORS, Base.metadata.create_all e routers)
- database.py (engine, Base, SessionLocal)
- models.py (tabelas principais: User, Company, Workout, Challenge, Reservation, Room, Instructor)
- models_additions.py (TokenTransaction, RewardLimit, VerificationEvent)
- routers/ (pasta com todos os endpoints)
  - auth.py
  - workouts.py
  - gamification.py
  - reports.py
  - rewards.py
  - calendar.py (NOVA)
  - reservations.py (NOVA)
  - planner.py (NOVA)

🧩 Adicione as seguintes novas funcionalidades:

1️⃣ **CALENDÁRIO (tab principal “Calendário”)**
- Criar modelo CalendarEvent com:
  - título, descrição, data, hora início/fim, tipo de evento, status (pendente, cumprida), lembrete (boolean), tarefas vinculadas.
- Endpoint CRUD para:
  - Criar evento, marcar como cumprido, adicionar lembrete e anexar tarefas.
- Interface API:
  - Filtros por semana, mês e um filtro adicional de datas personalizadas.
  - Endpoint para exportar dados (PDF/CSV) com base nas datas filtradas.
- Todos os calendários com 24 horas visíveis e agendamento livre.
- Backend otimizado para agendamentos e relatórios automáticos.

2️⃣ **RESERVA DE AULAS (tab “Reservas”)**
- Adicionar sub-aba vinculada à reserva da aula com:
  - lista de alunos (manual + import), data/hora da aula, nome da sala, instrutor e foto da aula.
  - ícone de e-mail para enviar mensagem a todos inscritos.
  - botão de “Reservar”, campos de presença (check ✓) e cancelamento.
  - contador de vagas (total/inscritos/presentes/faltas).
  - geração automática de gráfico circular (matplotlib) mostrando % de ocupação, presença e faltas.
- Filtros no topo: 
  - todas as aulas realizadas, todas as salas, todos os membros da equipe.
- Backend deve suportar envio de e-mails via SMTP (placeholders configuráveis).

3️⃣ **AGENDAMENTO DE AULA (tab “Agendar”)**
- Criar endpoint “create_class_event”:
  - Campos: nome da aula, instrutor, sala, data, hora, dia da semana, limite de inscrições, grupos permitidos.
  - Opções adicionais:
    - “Clientes precisam reservar esta aula” (boolean)
    - “Usar configurações padrão (15 dias antes)” (checkbox)
    - “Permitir reserva para grupos específicos” (checkbox)
- Filtros superiores:
  - “Todas as salas”, “Todas as aulas”, “Todos os membros da equipe”.

4️⃣ **PLANEJADOR (tab “Planejador”)**
- Sub-aba exclusiva (oculta por padrão).
- Ao clicar, abre um painel lateral que:
  - exibe mini visão do calendário e estatísticas de reservas.
  - fecha automaticamente ao clicar fora.
- Deve funcionar de forma integrada com as outras tabs (Calendário, Reservas e Agendar).

🧠 Requisitos adicionais:
- Estatísticas em tempo real de reservas (via WebSocket).
- Suporte para envio de e-mails em massa (placeholders no backend).
- Função para gerar relatórios PDF com gráficos circulares.
- Otimização UX: respostas rápidas e feedback visual (status 200 com mensagens amigáveis).
- Código limpo e modular, pronto para deploy.

🚀 Instruções finais:
1. Configure tudo em um único projeto funcional.
2. Gere os arquivos necessários automaticamente (app.py, routers, models, etc.).
3. Instale dependências:
   pip install fastapi uvicorn sqlalchemy jinja2 python-multipart pydantic[email] matplotlib

4. Rode o servidor com:
   uvicorn app:app --reload

Este backend deve gerar uma API REST completa e modular, compatível com frontend Next.js, e pronta para integrar o dashboard do Metavida App (Calendário + Reservas + Planejador).
